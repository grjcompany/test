<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 指明这个映射文件adminMapper.xml的逻辑路径,如果与接口建立映射，那么逻辑路径应该与接口的物理路径一致-->
<mapper namespace="com.lcvc.test.dao.OrderDao">
   <select id="readAll" resultType="Order">
       select * from Order
   </select>

<!--
    &lt;!&ndash; 处理登录操作 &ndash;&gt;
    <select id="login"  resultType="Order">
        select * from order where username=#{username} and password=#{password}
    </select>

    <delete id="delete" >
        delete from order where id=#{id}
    </delete>

    <select id="get"  resultType="Admin">
        select * from order where id=#{id}
    </select>

    <insert id="add">
        insert into order(id,username,password,name)
        values(null,#{username},#{password},#{name})
    </insert>
    <select id="readAll" resultType="Order">
        select * from admin order by id asc
    </select>-->
</mapper>